# Pinned to the exact versions used in development (Python 3.11+).
# Only direct dependencies are listed; transitive deps are resolved by pip
# from these pins and are deterministic for a given Python version.

# --- Web framework ---
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22

# --- OpenAI client + env loading ---
openai==2.29.0
python-dotenv==1.2.2

# --- File extraction ---
pdfplumber==0.11.9
python-docx==1.2.0
chardet==7.3.0

# --- Scoring / similarity math ---
scikit-learn==1.8.0
numpy==2.4.3

# --- Testing ---
pytest==9.0.2
